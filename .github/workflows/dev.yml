name: Publish Boilerplate Package in Maven Central Repository

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: write
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0 # Baixa todo o histórico de commits e tags

      - uses: actions/checkout@v4
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'corretto'
          server-id: github # Value of the distributionManagement/repository/id field of the pom.xml

      - name: Determine version scope
        run: bash ./scripts/reckon-auto-versioning

      - name: Current Tag
        run: |
          git fetch --tags

      - name: List tags and commits
        run: |
          git tag
          git log --oneline --all

      - name: Fetch complete history
        run: |
          git fetch --unshallow
          git fetch --tags

      - name: Publish to GitHub Packages
        run: ./gradlew clean publish reckonTagPush -Preckon.scope=$SCOPE -Preckon.stage=beta
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN  }}


        
